services:
  bfsiboard:
    image: ghcr.io/bfsiboardorg/bfsiboard:latest
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./repos:/data:ro
    environment:
      - BFSIBOARD_SCAN_PATH=/data
      - BFSIBOARD_SCAN_INTERVAL=900
    restart: unless-stopped
